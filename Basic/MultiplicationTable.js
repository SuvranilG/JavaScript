
const multiplicationTable=(no)=>{
    no = Number(no);
    for (let i = 1; i <= no; i++) {
        console.log(`\nMultiplication table for ${i} starts `)
        for (let j = 1; j <=10 ; j++) {
           console.log(`${i} X ${j}= ${i*j}`)
            
        }
    }
}

// multiplicationTable("9");

console.log(Number("s"));